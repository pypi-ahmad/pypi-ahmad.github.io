/**
 * Application Settings
 *
 * Global toggles and service IDs that affect the entire portfolio.
 * Edit these values to enable/disable features without touching component code.
 */
export const settings = {
  isSplash: false, // Change this to true if you want to use the splash screen.
  useCustomCursor: true, // Change this to false if you want the default cursor
  // GA4 Measurement ID format: G-XXXXXXXXXX
  googleTrackingID: "",
};
