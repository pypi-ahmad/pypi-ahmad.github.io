/**
 * Projects Data
 *
 * `projectsHeader`  — Title and description for the /projects page header.
 * `projects.data`   — Array of open-source project cards (currently empty;
 *                      the Projects page shows enterprise systems instead).
 *
 * To add a project, push an object with { name, url, description, languages }.
 */
export const projectsHeader = {
  title: "Projects",
  description:
    "My methodologies include Reasoning-Driven IDP, Agentic Workflows, and Enterprise RAG systems.",
  avatarImagePath: "projects_image.svg",
};

export const projects = {
  data: [],
};
