# Documentation Sync Report

- Direction: `code-to-docs`
- Scope: changed package, workflow, runtime, README, architecture, migration, and codebase-reference files
- Artifact root: repository-local `.aiwg/`; the `aiwg` command, `AIWG_ARTIFACTS_PATH`, and `.aiwg-location` redirect were unavailable

## Findings and applied fixes

1. Removed React Bootstrap from the README because it is absent from the manifest and source imports.
2. Documented all seven generated `build/<route>/index.html` fallbacks and corrected the architecture build check.
3. Documented all three accent values and visual variants consistently.
4. Distinguished GA4 initialization from React providers and documented its mount-time configuration gate and lack of explicit route pageview calls.
5. Removed the resolved stale-dependency concern and linked the detailed codebase reference set from the README.
6. Replaced the outdated statement that four CI gates run with a count-independent requirement that all CI gates pass.

## Files changed by this sync

- `README.md`
- `docs/architecture.md`
- `docs/codebase/CONCERNS.md`

Other existing documentation changes were audited and preserved.

## Validation

- `git diff --check` passed.
- Maintained-document relative links resolve locally.
- Dependency, accent, generated-route, and analytics assertions passed against current source and configuration.
- A fresh-context reader test passed local setup, architecture, fallback routes, theme persistence, analytics, CI/deployment, migration status, and human-review questions.
- A second fresh-context reader retest passed all corrected drift checks.

## Remaining human review

- Decide whether the manual `npm run deploy` / `gh-pages` publication path remains supported alongside the GitHub Actions Pages workflow.
- External hosting state, deployed revision, branch/environment protections, Vercel ownership, and access controls require live platform verification.
- Decide whether `docs/codebase/.codebase-scan.txt` should remain an untracked transient inventory or become a filtered maintained artifact.

## Detailed evidence

- `.aiwg/working/doc-sync/public-docs.md`
- `.aiwg/working/doc-sync/codebase-reference.md`
