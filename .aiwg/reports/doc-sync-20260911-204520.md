# Documentation Sync Report

- Direction: code-to-docs
- Scope: changed workflow, package, runtime, README, migration, and codebase documentation files
- Artifact-root resolution: `aiwg artifacts path` was unavailable; no `AIWG_ARTIFACTS_PATH`, `.aiwg-location`, or existing `.aiwg` redirect was found, so this local artifact directory was used.

## Findings and applied fixes

1. README still described CI as a basic test gate and the Astro roadmap as active. Updated it to document coverage, Chromium browser checks, and the retired roadmap.
2. Architecture, stack, structure, conventions, integrations, and testing guides had stale browser-gate or coverage details. Updated them to match `ci.yml`, `vitest.config.js`, and `package.json`.
3. Testing documentation claimed 31 direct dependencies; the manifest has 32. Corrected the count.

## Validation

- Documentation assertions for the updated CI, coverage, and retirement statements passed.
- `git diff --check` passed.
- `npm run test:coverage` passed: 22 files, 173 tests; 86.59% statements, 77.08% branches, 77.77% functions, 86.76% lines.

## Remaining review

No content conflicts remain. The new browser gate has passed locally but has not yet run in GitHub Actions.
