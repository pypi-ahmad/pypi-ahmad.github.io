# Documentation Sync Report

- Direction: code-to-docs
- Scope: derived from current git changed files (no `--scope`/`--incremental` given) — `CHANGELOG.md`,
  `README.md`, `docs/architecture.md`, `docs/codebase/CONVENTIONS.md`, `docs/codebase/TESTING.md`
  (already modified from the prior `doc-sync-20260912-024050` run), plus `docs/TECHNICAL.md` and
  `docs/RUNBOOK.md` (new files added earlier this session, not yet audited by any doc-sync pass).
- Artifact-root resolution: `aiwg artifacts path` is unavailable (`aiwg: command not found`); no
  `AIWG_ARTIFACTS_PATH` or `.aiwg-location` redirect exists, so this local `.aiwg/` directory was used
  (consistent with all three prior runs recorded here).
- Context: no application/source code changed since the last sync (`doc-sync-20260912-024050`, which
  already reconciled the accent-removal/font-swap/palette drift). Three more commits landed since then:
  `c96e9df` (enlarge homepage eyebrow — same category as the already-checked `5147567`, no doc impact
  beyond wording completeness), `14e418e` (added `vercel.json`'s `git.deploymentEnabled.gh-pages: false`),
  and `5e7241a` (manually triggered Vercel production deploy, no code change). This run also self-audited
  the two new hand-written docs (`docs/TECHNICAL.md`, `docs/RUNBOOK.md`) against current source.

## Findings and applied fixes

1. `CHANGELOG.md`'s "enlarged page section labels" bullet described `5147567` (five `src/pages/*.css`
   files) but not the separate, later `c96e9df` commit enlarging the homepage hero eyebrow
   (`src/containers/greeting/Greeting.css`, a different file outside `src/pages/`). Broadened the
   wording to name the homepage eyebrow explicitly.
2. `vercel.json` gained `git.deploymentEnabled.gh-pages: false` in `14e418e`, after the last sync.
   `docs/codebase/INTEGRATIONS.md` didn't mention it (not wrong, just written before the field existed).
   Added a sentence recording that Vercel's Git integration no longer auto-deploys from the `gh-pages`
   branch.
3. `README.md`'s Vercel deployment section already said to configure Vercel against `main` rather than
   the `gh-pages` branch, but predated the config-level enforcement of that. Added one sentence noting
   `vercel.json` now enforces it directly.
4. `CHANGELOG.md` had no entry for the Vercel Git-deployment change at all (a real, user-relevant
   deployment-behavior change, matching the project's own precedent of changelogging deployment-path
   changes, e.g. v2.4.0's GitHub Pages Actions migration). Added an Unreleased bullet.

## Self-audit: this session's new docs

`docs/TECHNICAL.md` and `docs/RUNBOOK.md` (written earlier this session) were re-checked line-by-line
against current source (`src/themeController.jsx`, `src/theme.js`, `src/components/ErrorBoundary.jsx`,
`src/components/seo/RouteMeta.jsx`, `package.json`, `vite.config.js`, `vercel.json`,
`.github/workflows/ci.yml`, `.github/workflows/deploy.yml`). No drift found; every specific claim
(invariant, error-handling behavior, persistence key, CI poll loop bound, deploy trigger) traces to a
line I re-read in this pass.

## Validation

- `git diff --check` on the three edited files: passed (no whitespace conflicts).
- No source/application code was touched, so `lint`/`typecheck`/`build`/`test` do not apply to this
  pass; the prior run (`doc-sync-20260912-024050`) already validated the still-unchanged application
  code these docs describe (170 tests passed, coverage 89.63%/82.47%/81.87%/89.34%, lint/typecheck/build
  passed).
- Every fact cited above was independently verified against the current on-disk file in this pass
  (git log/show for the three new commits, direct read of `vercel.json`), not carried over from the
  prior report without recheck.

## Remaining review

- `knowledge/architecture/portfolio.md` still describes the retired 3-accent theme system (carried over
  from the prior report; still an `okf` skill regeneration task, not a doc-sync edit).
- Hosting-account-level Vercel/GitHub Pages settings (branch protection, Pages environment protection,
  actual Vercel dashboard Git-integration configuration) remain outside the repository and unverifiable
  from source, per `docs/codebase/INTEGRATIONS.md` and `docs/RUNBOOK.md`.
- Earlier in this session a mistaken full rewrite of `README.md` (from an unrelated documentation
  request) was caught and reverted with `git restore` before this sync ran; `README.md`'s only change
  in this pass is the one-sentence Vercel addition above.

## Changed files

- CHANGELOG.md
- README.md
- docs/codebase/INTEGRATIONS.md
